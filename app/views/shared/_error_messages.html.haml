- if target.errors.any?
	%div#errorExplanation
		%h4 #{pluralize(target.errors.count, "error")} prohibited this record from being saved:
		%ul
		- target.errors.full_messages.each do |msg|
			%li= msg
