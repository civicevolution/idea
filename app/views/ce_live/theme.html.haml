- content_for(:script) do
	:javascript
		$('a.new_live_talking_point').live('click', 
			function(){
				console.log("new_live_talking_point - add another form v1");
				var tp = $('div.talking_point_block'); 
				var tp_clone = tp.eq(0).clone();
				if(tp.size() > 1){
					$('a.new_live_talking_point').hide();
				
				}else{
				
				}
				tp_clone.find('textarea').val('');
				tp_clone.find('h3').html( tp_clone.find('h3').html().replace('Enter a ', 'Start a new ') );
				tp.last().after(tp_clone)
				return false;
			}
		)
	
	%style
		:sass
			div.page
				div#left
					width: 400px
					float: left
					margin-right: 40px
				div#right
					width: 400px
					float: left

			div#outer_chat
				div#chat_log
					overflow: auto
					overflow-y: scroll
					width: 460px
					height: 300px
					border: 1px solid black
					padding: 1em 0
					p 
						margin: 0 1em
				input[type=text]
					width: 360px

			div#live_talking_points
				div.inner
					overflow: auto
					overflow-y: scroll
					width: 460px
					height: 300px
					border: 1px solid black
					padding: 1em 0
				input[type=text]
					width: 360px
			


%div#theme
	- @title = 'Theme team page - CivicEvolution Live'
	%h3 Theme team page @ CivicEvolution Live


	%h3 Talking points from the tables
	%div#live_talking_points
		%div.inner


	%h3 Chat with your tables
	%div#outer_chat			
		%div#chat_log
		= form_tag( chat_path, {:class=>'chat_form', :remote=>true}) do
			= hidden_field_tag :team_id, params[:team_id]
			= text_field_tag :msg
			= submit_tag 'Send', {:disable_with => 'Submitting…'}
