%div#vote_saved
	- if status == 'failed'
		- if err_msgs && !err_msgs.empty?
			%p Sorry, we could not save your votes because:
			%ul
				- err_msgs[:full_messages].each do |msg|
					%li= msg
		- else
			%p We had a problem saving your vote. We have been notified and will look into this very soon. 
			%p We apologize for this inconvenience and we will contact you via email if appropriate

	- else
		%div#submit_proposal_acknowledge
			%p Thank you, your vote has been saved