%div.member_info
	%div.member
		= link_to edit_profile_form_path(@member.ape_code) do
			= image_tag @member.photo.url('36'), {:class=>'i36 signin_pic', :title=>'Click to upload your photo'}
		#{@member.first_name} #{@member.last_name}
