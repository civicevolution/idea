%div.edit_form			
	= form_tag(edit_theme_path(params[:idea_id]), :remote=>true, :class=>'std_form orig theme_edit', :id=>params[:idea_id]) do
		= hidden_field_tag :act, 'edit_theme_popup'
		= hidden_field_tag :question_id, params[:question_id]

		.wmd-panel
			#wmd-button-bar
			- text = idea.version == 0 ? '' : idea.text
			= text_area_tag :text, text, {id: 'wmd-input', class: 'wmd-input autosize', placeholder: 'Write your answer here...'}

		#wmd-preview.wmd-panel.wmd-preview

		%p.form_controls.no_attach
			= submit_tag "Update", 'data-disable-with'=>'Submitting...'
			= link_to 'Discard changes', {}, class: 'cancel', rel: 'nofollow'