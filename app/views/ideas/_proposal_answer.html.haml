%li.theme{id: theme.id}	
	%div.answer
		:markdown
			#{theme.text}
			
	%div.answer_status	
		%div.comments
			= image_tag('comment-sketch.gif', title: 'Number of comments')
			%div.counts
				%div.new
					0 new
				%div.total
					0 total
		%p Click to rate and discuss
