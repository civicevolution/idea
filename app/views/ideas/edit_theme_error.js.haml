= render :template => 'shared/clear_sign_in_form', :formats => [:js]
:plain
	var errors = $("#{escape_javascript( render :partial => 'shared/error_message', :locals => {:target => idea})}");
- if params[:act] == "theme_final_edit"
	:plain
		console.log("show the errors: " + errors);
		var form = $('div.theme_final_edit li.theme[theme_id="#{idea.id}"] form');
		form.append(errors);
- else
	:plain
		var form = $('form.edit_theme[id="#{idea.id}"]');
		form.find('p.warn').remove();
		form.find('div#errorExplanation').remove();	
		form.append(errors);
