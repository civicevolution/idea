%div#initiative_subscribed
	- if @act == 'remove'
		%h3 We will unsubscribe you
	- else 
		%h3 Thank you for subscribing to NCDD Catalyst Awards Project
		
	= link_to 'Close', home_path, {:class=>'cancel'}