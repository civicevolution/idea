:plain
	var com = $("#{escape_javascript(render(  :partial=> 'plan/comment', :locals=>{:comment=>comment, :members => members}, :content_type => 'html'  ))}");
	$('div.question_worksheet[id=#{question_id}] div.talking_point_comments[id=#{comment.parent_id}] div.Comment:last').after(com);
