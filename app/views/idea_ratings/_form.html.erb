<%= form_for(@idea_rating) do |f| %>
  <% if @idea_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@idea_rating.errors.count, "error") %> prohibited this idea_rating from being saved:</h2>

      <ul>
      <% @idea_rating.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :idea_id %><br />
    <%= f.number_field :idea_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br />
    <%= f.number_field :member_id %>
  </div>
  <div class="field">
    <%= f.label :rating %><br />
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
